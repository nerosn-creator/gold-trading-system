# 重要事件記錄與 Vercel 部署手冊 (Important Events & Vercel Deployment)

本文件記錄 `Gold Trading System`（黃金交易訊號系統）的架構與 Vercel 雲端部署設定，比照 `mom-deposit-app` 統一管理。

---

## 🚀 Vercel 雲端架構與運作原理

1. **Serverless 架構**
   - **後端 API**：由 `api/index.py` 作為 Serverless Function 進入點，使用 `@vercel/python` Runtime 自動驅動 FastAPI (`backend/main.py`)。
   - **前端靜態檔案**：位於 `frontend/`，由 `vercel.json` 負責靜態檔案路由 (`/static/*`, `/`, `/manifest.json`)。
   - **依賴管理**：由根目錄 `requirements.txt` 定義，Vercel 部署時會自動安裝所需套件。

2. **一鍵推送到 GitHub (CI/CD 連動)**
   - 專案已配備 `update_github.bat`。
   - 只要點擊執行 `update_github.bat`，即可一鍵將本機最新修改推送到 GitHub `main` 分支。
   - Vercel 偵測到 GitHub 更新後，將自動觸發重新建置與上線。

3. **輕量化部署優化**
   - 透過 `.vercelignore` 排除本機的 `.exe`、日誌與本機腳本，避免上傳超額二進位檔案，加速部署。
   - 檔案儲存相容：在無伺服器唯讀環境下自動相容 `/tmp` 臨時目錄。

---

## 📅 重要修改紀錄 (Changelog)

### [2026-08-25] - 配置 Vercel 部署設定檔
* **新增 `api/index.py`**：Vercel Python Serverless 進入點，自動橋接 FastAPI 應用。
* **新增 `vercel.json`**：設定 Python Serverless 路由與前端靜態資源代理。
* **新增 `.vercelignore`**：排除本機二進位檔案（`ngrok.exe`, `cloudflared.exe` 等）以加速部署。
* **完善 `requirements.txt`**：補充 `pytz` 與相關相依套件。
* **相容 Serverless 環境**：`main.py` 與 `strategy_optimizer.py` 加入 `/tmp` fallback 機制，避免檔案讀寫權限報錯。
* **新增 `update_github.bat`**：比照 mom-deposit-app 提供一鍵推送更新 GitHub 工具。
